let listElemets = document.querySelectorAll( ".list--click");

tElemets.forEach(listElemets => {
    listElemet.addEventListener("click",()=> {
     
        listElemet.classlist.toggle("arrow");

        let height = 0;

        let menu = listElemet .nextElementsibling;
        console.log()
        if(menuclientHeight == "0"){
            height=menuscrollheight;
        }

        menustyleheight = "${height}px";
    })
    
});